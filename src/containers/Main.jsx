import React from 'react'
import { Link } from 'react-router-dom'
import Navigation from '../components/Navigation.jsx'

export default (props) => (
  <div>
    <Navigation />
    <p>Just a demo to see what can be done</p>
  </div>
)
